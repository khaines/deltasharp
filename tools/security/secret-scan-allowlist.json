{
  "_comment": [
    "Secret-scan allowlist for DeltaSharp — STORY-00.3.1 (#107).",
    "Consumed by tools/security/secret-scan.py and the 'secret-scan' job in",
    ".github/workflows/security.yml. See docs/engineering/design/supply-chain-security.md.",
    "",
    "Each allow[] entry suppresses a KNOWN, reviewed match (a test fixture or a documented",
    "false positive). EVERY entry MUST carry path, scope, reason, owner, and expiry",
    "(YYYY-MM-DD). 'path' is an fnmatch glob relative to the repo root; 'rules' is a list of",
    "detector ids (or ['*'] for any). SCOPE 'rules' to only the detectors the file legitimately",
    "contains — never ['*'] on a fixture — so that a NEW provider secret added to the file is",
    "NOT silently suppressed but surfaces as a violation. An EXPIRED entry stops suppressing —",
    "the match re-surfaces and fails the scan — so waivers cannot rot. A real leaked secret must",
    "be ROTATED, never allowlisted."
  ],
  "allow": [
    {
      "path": "tools/security/testdata/secret-scan-fixture.txt",
      "rules": ["aws-access-key-id", "deltasharp-test-canary"],
      "scope": "repository test fixture (single file)",
      "reason": "Intentional non-functional fake-credential fixture that proves the secret detectors fire on every run; contains no real secret. 'rules' is scoped to exactly the two detectors the fixture carries so a NEW provider secret (e.g. a github-token) added here would FAIL the scan instead of being masked.",
      "owner": "@khaines",
      "expiry": "2027-07-04",
      "reviewCriteria": "On expiry, re-confirm the fixture is still fake and still needed; remove the entry if the in-repo scanner is retired. If a detector is added to the fixture, add its rule id here (deliberately)."
    }
  ]
}
